# LegalEase Environment Variables
# Copy this file to .env and update the values

# ============================================
# QUICK START (Minimal Local Setup)
# ============================================
# For local development with Firebase emulators, you only need ONE thing:
#
#   GOOGLE_GENAI_API_KEY=your-api-key
#
# Get your free API key from: https://aistudio.google.com/apikey
# Then run: mise run dev:local
#
# That's it! Everything else below is optional or for production.
# ============================================

# ============================================
# Firebase Configuration (for production)
# ============================================
# Get these from Firebase Console > Project Settings

FIREBASE_PROJECT_ID=your-project-id
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# ============================================
# Google Cloud Platform
# ============================================

# GCP project ID (usually same as Firebase project ID)
GOOGLE_CLOUD_PROJECT=your-project-id
GCP_REGION=us-central1

# GCS bucket (usually same as Firebase storage bucket)
GCS_BUCKET=your-project.firebasestorage.app

# ============================================
# AI/ML Services
# ============================================

# Google AI API key (for Gemini via Genkit and transcription)
# Get from: https://aistudio.google.com/apikey
GOOGLE_GENAI_API_KEY=

# Transcription provider configuration
# Options: gemini, chirp (default: gemini)
TRANSCRIPTION_PROVIDER=gemini

# Gemini model for transcription (default: gemini-2.5-flash)
# TRANSCRIPTION_GEMINI_MODEL=gemini-2.5-flash

# Optional: OpenAI API key (for GPT models in Genkit)
# OPENAI_API_KEY=

# Optional: Anthropic API key (for Claude models in Genkit)
# ANTHROPIC_API_KEY=

# ============================================
# Vector Database (Qdrant)
# ============================================

# Use local Qdrant for development
QDRANT_LOCAL=true
QDRANT_URL=http://localhost:6333

# For cloud Qdrant (production)
# QDRANT_LOCAL=false
# QDRANT_URL=https://your-cluster.qdrant.io
# QDRANT_API_KEY=your-api-key

# ============================================
# Document Processing (Docling)
# ============================================

# Docling service URL (local Docker or Cloud Run)
DOCLING_SERVICE_URL=http://localhost:5050

# Request timeout in milliseconds (default: 600000 = 10 minutes)
# DOCLING_TIMEOUT=600000

# Skip OCR processing (default: false)
# DOCLING_SKIP_OCR=false

# Skip table structure detection (default: false)
# DOCLING_SKIP_TABLE_STRUCTURE=false

# ============================================
# Local Development
# ============================================

# HuggingFace API Token (for Pyannote model downloads)
# HF_TOKEN=

# Path to forensic exports (optional)
# FORENSIC_EXPORTS_PATH=/data/forensic-exports
