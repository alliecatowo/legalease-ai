# LegalEase Frontend Environment Variables
# Copy this file to .env and update the values

# ============================================
# MINIMAL LOCAL SETUP (just need these!)
# ============================================
# For local development with Firebase emulators, you only need:
# 1. NUXT_PUBLIC_USE_EMULATORS=true
# 2. GOOGLE_GENAI_API_KEY (get from https://aistudio.google.com/apikey)
# That's it! The emulators handle Auth, Firestore, Storage, and Functions.

NUXT_PUBLIC_USE_EMULATORS=true

# ============================================
# Firebase Configuration (for production)
# ============================================
# Get these from Firebase Console > Project Settings > General > Your apps
# Not needed when using emulators, but required for production deployment.

FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# ============================================
# Transcription Provider
# ============================================
# Controls which transcription provider to use:
# - gemini (default): Uses Gemini 2.5 Flash, works with Storage emulator
# - chirp: Uses Google Speech-to-Text, requires production GCS

TRANSCRIPTION_PROVIDER=gemini

# ============================================
# Optional Settings
# ============================================

# Public site URL (for OG images, etc.)
NUXT_PUBLIC_SITE_URL=http://localhost:3000
